# PostgreSQL Integration

Place configuration files (e.g. Metricbeat `postgresql.yml` or Elastic Agent policy JSON)
for monitoring PostgreSQL here. Include any DSN templates and notes on required alerts.

