# NGINX Integration

This directory should contain metrics/logs configuration for the NGINX Ingress Controller.
A typical `nginx.yml` for Metricbeat or Elastic Agent NGINX integration goes here.

Alert rule exports related to LB metrics can also be stored in `../alerting-rules`.
