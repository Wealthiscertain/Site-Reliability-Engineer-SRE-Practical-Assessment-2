# Redis Integration

Configuration and credentials for the Elastic Agent or Metricbeat Redis module should be stored here.
Include sample `redis.yml` or policy JSON and notes on alert rules (memory, eviction, etc.).
